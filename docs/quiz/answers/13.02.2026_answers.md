# Klucz odpowiedzi – 13.02.2026

| Pytanie | Poprawna | Uzasadnienie |
|---------|----------|--------------|
| 1  | C | response_model=List[UserDisplay] powoduje serializację według schematu bez hasła. |
| 2  | A | prefix to wspólny fragment URL dla endpointów routera. |
| 3  | B | SQLite z check_same_thread=False pozwala na użycie połączenia w wielu wątkach (FastAPI). |
| 4  | B | None dałoby 200 OK z null; HTTPException(404) daje poprawny status i komunikat. |
| 5  | A | orm_mode = True umożliwia konwersję z obiektów SQLAlchemy do Pydantic. |
| 6  | C | FastAPI rozpoznaje Path/Query/Body po kontekście, nie kolejności. |
| 7  | B | create_article rzuca StoryException; exception_handler zwraca np. 418 z JSON. |
| 8  | D | yield w get_db tworzy generator; kod po yield (db.close()) wykonuje się po zakończeniu żądania. |
| 9  | A | response_model określa format odpowiedzi API (serializacja według schematu). |
| 10 | C | Brak dopasowania ścieżki → endpoint nie jest wywołany → walidacja się nie wykonuje. |
| 11 | B | UserBase (request z hasłem), UserDisplay (response bez hasła) – bezpieczeństwo. |
| 12 | D | create_all() tworzy brakujące tabele; nie aktualizuje istniejących. |
| 13 | A | Parametr typu BaseModel jest traktowany jako Body. |
| 14 | D | Router product zwraca PlainTextResponse z status_code 404 przy IndexError. |
| 15 | C | Hash służy do hashowania haseł (bcrypt) i weryfikacji. |
| 16 | D | Tag grupuje endpointy w Swagger UI; nie zmienia URL. |
| 17 | A | orm_mode i relationship umożliwiają serializację zagnieżdżonego user do JSON. |
| 18 | B | back_populates łączy dwie strony relacji (user.items, article.user). |
| 19 | C | exception_handler zwraca np. JSONResponse z dowolnym kodem i content; spójna odpowiedź na wyjątek. |
| 20 | D | Modele ORM SQLAlchemy nie mają update(); zmiany przez przypisanie atrybutów i commit(). |

Walidacja: każda litera (A, B, C, D) występuje jako poprawna; max 2 takie same pod rząd.
