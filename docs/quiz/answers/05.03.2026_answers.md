# Klucz odpowiedzi – 05.03.2026

| Pytanie | Poprawna | Uzasadnienie |
|---------|----------|--------------|
| 1  | C | response_model określa schemat serializacji odpowiedzi; w JSON trafiają tylko pola z tego schematu. |
| 2  | A | prefix to wspólny fragment URL dla wszystkich endpointów zarejestrowanych w tym routerze. |
| 3  | B | SQLite domyślnie blokuje połączenie spoza wątku twórcy; check_same_thread=False pozwala na użycie silnika w wielu wątkach. |
| 4  | B | Header() służy do wstrzykiwania wartości z nagłówków HTTP jako parametrów endpointu. |
| 5  | A | orm_mode (from_attributes) pozwala budować schemat Pydantic z obiektów ORM (odczyt atrybutów). |
| 6  | C | FastAPI rozpoznaje Path/Query/Body po kontekście (URL, typ, Body()), nie po kolejności parametrów. |
| 7  | B | exception_handler przyjmuje (request, exc) i zwraca Response (np. JSONResponse); definiuje spójną odpowiedź na wyjątek. |
| 8  | D | yield tworzy generator; kod po yield (np. finally: db.close()) wykonuje się po zakończeniu obsługi żądania. |
| 9  | A | response_model definiuje schemat odpowiedzi – serializacja i walidacja wyjścia. |
| 10 | C | Najpierw dopasowanie ścieżki; bez dopasowania endpoint nie jest wywołany, więc walidacja się nie wykonuje. |
| 11 | B | Osobne schematy request/response pozwalają nie ujawniać haseł w API (bezpieczeństwo, kontrakt). |
| 12 | D | create_all() tworzy brakujące tabele z modeli; nie aktualizuje ani nie usuwa istniejących. |
| 13 | A | Parametr typu BaseModel jest automatycznie traktowany jako Body (JSON). |
| 14 | D | 404 z czytelnym komunikatem to standardowa praktyka przy braku zasobu. |
| 15 | C | Hash (bcrypt) – jednokierunkowe hashowanie; zapis hasha, weryfikacja przez porównanie z hashem. |
| 16 | D | tags służy tylko do grupowania w dokumentacji (Swagger); nie zmienia URL ani logiki. |
| 17 | A | orm_mode + relationship w ORM umożliwiają serializację zagnieżdżonych obiektów według schematów Pydantic. |
| 18 | B | back_populates łączy dwie strony relacji (np. user.items ↔ article.user). |
| 19 | C | exception_handler zwraca obiekt Response (np. JSONResponse) z dowolnym status_code i content. |
| 20 | D | Modele ORM nie mają update(); zmiany przez przypisanie atrybutów; ORM generuje UPDATE przy commit(). |

Walidacja rozkładu: A=5, B=5, C=5, D=5; każda odpowiedź przynajmniej raz; max 2 takie same pod rząd.
