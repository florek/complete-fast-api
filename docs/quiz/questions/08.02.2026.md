# Quiz FastAPI - 08.02.2026

## Pytanie 1
Co robi `tags=['blog']` w `APIRouter()`?
- A) Ogranicza liczbę requestów na sekundę
- B) Wymusza autentykację
- C) Grupuje endpointy w dokumentacji Swagger UI
- D) Zmienia prefix URL

## Pytanie 2
Która metoda jest używana do pobranie pojedynczego rekordu z bazy w SQLAlchemy, gdy oczekujemy co najwyżej jednego wyniku?
- A) `.all()`
- B) `.first()`
- C) `.one()`
- D) `.single()`

## Pytanie 3
Do czego służy `Depends(get_db)` w parametrze endpointu?
- A) Do walidacji body requestu
- B) Do ustawienia nagłówków odpowiedzi
- C) Do cache'owania odpowiedzi
- D) Dependency Injection – wstrzyknięcie sesji bazy danych

## Pytanie 4
Co oznacza `ForeignKey('users.id')` w modelu SQLAlchemy?
- A) Klucz obcy – powiązanie z inną tabelą
- B) Indeks pełnotekstowy
- C) Unikalne pole
- D) Klucz główny

## Pytanie 5
Jak w FastAPI zwracamy błąd 404 z komunikatem?
- A) `return None`
- B) `return 404`
- C) `response.status_code = 404`
- D) `raise HTTPException(status_code=404, detail='Not found')`

## Pytanie 6
Co zwraca `db.add(obj)` przed wywołaniem `db.commit()`?
- A) Nic (tylko dodaje do sesji)
- B) ID nowego rekordu
- C) Obiekt po zapisie
- D) Liczbę zmienionych wierszy

## Pytanie 7
Do czego służy `response_model=UserDisplay` w dekoratorze endpointu?
- A) Waliduje tylko request
- B) Wymusza metodę POST
- C) Określa format odpowiedzi i serializację do JSON
- D) Ustawia kod statusu 200

## Pytanie 8
Co to jest `back_populates` w `relationship()`?
- A) Opcja do sortowania wyników
- B) Powiązanie drugiej strony relacji (relacja dwukierunkowa)
- C) Filtr zapytania
- D) Nazwa kolumny w bazie

## Pytanie 9
Który typ parametru w FastAPI pochodzi z ścieżki URL, np. `/user/{id}`?
- A) Query
- B) Body
- C) Header
- D) Path

## Pytanie 10
Co robi `db.refresh(obj)`?
- A) Usuwa obiekt z sesji
- B) Odświeża obiekt danymi z bazy (np. po commit)
- C) Zapisuje obiekt do bazy
- D) Tworzy kopię obiektu

## Pytanie 11
Dlaczego hasła haszujemy przed zapisem do bazy?
- A) Bezpieczeństwo – nie przechowujemy hasła w plaintext
- B) Aby przyspieszyć logowanie
- C) Aby zmniejszyć rozmiar bazy
- D) FastAPI tego wymaga

## Pytanie 12
Jak w Pydantic oznaczamy pole wymagane (bez wartości domyślnej)?
- A) `field: str`
- B) `field: str = None`
- C) `field: str = ...` (Ellipsis)
- D) `field: Optional[str]`

## Pytanie 13
Co to jest `SessionLocal` w konfiguracji SQLAlchemy?
- A) Jedna globalna sesja
- B) Fabryka sesji ORM – tworzy nowe sesje
- C) Connection string
- D) Nazwa tabeli

## Pytanie 14
Który kod statusu HTTP oznacza „zasób nie znaleziony”?
- A) 404
- B) 400
- C) 401
- D) 500

## Pytanie 15
Jak aktualizujemy rekord w SQLAlchemy (bez raw SQL)?
- A) `db.update(Model).where(...)`
- B) Tylko przez `db.execute()`
- C) SQLAlchemy nie pozwala na update
- D) Pobieramy obiekt, zmieniamy atrybuty, wywołujemy `db.commit()`

## Pytanie 16
Do czego służy `List[UserDisplay]` jako `response_model`?
- A) Ogranicza długość listy
- B) Mówi FastAPI, że odpowiedź to lista obiektów UserDisplay
- C) Sortuje wyniki
- D) Filtruje pola

## Pytanie 17
Skąd pochodzi `Base` w `class DbUser(Base)`?
- A) Z FastAPI
- B) Z Pydantic
- C) Z `db.database` – klasa bazowa dla modeli SQLAlchemy
- D) Z standardowej biblioteki Python

## Pytanie 18
Po czym rozpoznajemy w FastAPI, że parametr to Body (dane JSON)?
- A) Gdy jest typu BaseModel lub opakowany w Body()
- B) Po nazwie `body`
- C) Zawsze ostatni parametr
- D) Gdy ma adnotację typing

## Pytanie 19
Co oznacza `check_same_thread=False` w konfiguracji SQLite?
- A) Wyłącza transakcje
- B) Wymusza jeden wątek
- C) Włącza cache
- D) Umożliwia użycie jednego połączenia z wielu wątków

## Pytanie 20
Zagnieżdżony model Pydantic w innym modelu (np. `user: User` w `ArticleDisplay`) służy do:
- A) Walidacji tylko requestu
- B) Cache'owania
- C) Opisu zagnieżdżonej struktury JSON (np. relacji)
- D) Szyfrowania pól
