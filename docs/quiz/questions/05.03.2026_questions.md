# Quiz FastAPI – 05.03.2026

1. **[łatwe] [Pydantic]** Gdy endpoint zwraca obiekty ORM zawierające pole password, a w dekoratorze ustawiono response_model na schemat Pydantic bez tego pola – skąd klient wie, że odpowiedź nie zawiera hasła?
   - A) ORM domyślnie nie zwraca kolumn oznaczonych jako secret
   - B) Middleware usuwa wrażliwe pola przed wysłaniem
   - C) FastAPI serializuje odpowiedź według response_model, więc w JSON trafiają tylko pola z tego schematu
   - D) Router musi jawnie wykluczyć pole przed return

2. **[łatwe] [FastAPI]** Do czego służy parametr prefix w APIRouter(prefix='/user', tags=['user'])?
   - A) Wspólny fragment URL dla wszystkich endpointów tego routera (np. /user/, /user/{id})
   - B) Ustawia domyślny tag w Swagger
   - C) Wymusza autoryzację dla endpointów pod tym prefiksem
   - D) Określa wersję API (v1, v2)

3. **[średnie] [SQLAlchemy]** Dlaczego przy użyciu SQLite z FastAPI często ustawia się connect_args={"check_same_thread": False}?
   - A) Aby włączyć tryb tylko do odczytu
   - B) SQLite domyślnie zezwala na połączenie tylko z wątku, który je utworzył; FastAPI obsługuje wiele żądań, więc silnik może być używany z różnych wątków
   - C) Aby wyłączyć transakcje
   - D) Aby umożliwić zapis z wielu procesów do tego samego pliku

4. **[średnie] [FastAPI]** Skąd w FastAPI endpoint ma brać wartość przekazaną w nagłówku HTTP (np. X-Custom)?
   - A) Parametr z Query(alias='X-Custom')
   - B) Parametr z Header() (np. x_custom: Optional[str] = Header(None))
   - C) Parametr z Body()
   - D) Parametr z Cookie()

5. **[łatwe] [Pydantic]** Co zmienia ustawienie orm_mode = True w schemacie Pydantic (Config)?
   - A) Umożliwia tworzenie instancji schematu z obiektów ORM (np. SQLAlchemy) przez odczyt atrybutów, zamiast tylko ze słownika
   - B) Włącza walidację regex na polach
   - C) Wymusza odpowiedź w formacie binarnego ORM
   - D) Ustawia tryb tylko do odczytu dla pól

6. **[średnie] [FastAPI]** Skąd FastAPI wie, że dana wartość ma pochodzić z Path, Query czy Body, skoro wszystkie są argumentami tej samej funkcji?
   - A) Z kolejności parametrów w sygnaturze (pierwszy = Path, drugi = Query itd.)
   - B) Z konwencji nazewnictwa (id_ path, reszta query)
   - C) Z kontekstu: {x} w URL → Path, BaseModel/Body() → Body, reszta → Query; kolejność nie ma znaczenia
   - D) Wszystko oprócz liczbowego id pochodzi z body JSON

7. **[średnie] [FastAPI]** Gdy w logice tworzenia zasobu rzucany jest niestandardowy wyjątek (Exception), a w aplikacji zarejestrowano dla niego exception_handler – co dostaje klient?
   - A) Zawsze 500 i stack trace
   - B) Odpowiedź zdefiniowaną w handlerze (np. JSONResponse z dowolnym kodem, np. 418, i treścią); handler przyjmuje request i exc
   - C) Odrzucenie żądania przed wejściem do logiki (422)
   - D) Pustą odpowiedź 204

8. **[trudne] [FastAPI]** Dlaczego dependency udostępniająca sesję bazy danych używa yield zamiast return?
   - A) Aby cache'ować sesję między żądaniami
   - B) yield jest składniowo wymagany przez Depends()
   - C) Aby umożliwić asynchroniczne odczyty
   - D) Generator pozwala wykonać kod po yield (np. db.close()) po zakończeniu obsługi żądania, więc sesja jest zawsze zamykana

9. **[łatwe] [FastAPI]** Do czego służy response_model w dekoratorze endpointu FastAPI?
   - A) Określa schemat, według którego odpowiedź jest serializowana i walidowana (np. wyklucza pola niewidoczne w schemacie)
   - B) Wymusza, aby body żądania było zgodne z tym modelem
   - C) Ustawia nagłówek Content-Type na application/json
   - D) Włącza walidację po stronie klienta

10. **[średnie] [FastAPI]** Co oznacza zasada „routing ważniejszy niż walidacja” w FastAPI?
    - A) Walidacja zawsze wykonuje się przed dopasowaniem ścieżki
    - B) Trzeba najpierw zwalidować nagłówki, potem ścieżkę
    - C) Jeśli URL nie pasuje do żadnej ścieżki, endpoint nie zostanie wywołany i walidacja parametrów tego endpointu w ogóle się nie wykona
    - D) Walidacja ma wyższy priorytet niż ustawiony status code

11. **[średnie] [API]** Gdy endpoint zamiast raise HTTPException(404) zwróci None przy braku zasobu, klient typowo otrzyma:
    - A) 404 Not Found
    - B) 500 Internal Server Error
    - C) 204 No Content
    - D) 200 OK z body null (lub pusty JSON)

12. **[trudne] [SQLAlchemy]** Co robi wywołanie Base.metadata.create_all(bind=engine) przy starcie aplikacji?
    - A) Tworzy wyłącznie tabele wymienione w konfiguracji
    - B) Uruchamia migracje Alembic (wersjonowanie schematu)
    - C) Kasuje istniejące tabele i tworzy je na nowo
    - D) Tworzy wszystkie tabele z modeli dziedziczących po Base, jeśli nie istnieją; nie modyfikuje ani nie usuwa istniejących tabel

13. **[łatwe] [FastAPI]** Skąd FastAPI wie, że dane w żądaniu POST mają pochodzić z body (JSON)?
    - A) Typ parametru: jeśli to model Pydantic (BaseModel), FastAPI traktuje go jako Body
    - B) Z kolejności parametrów (pierwszy = body)
    - C) Trzeba jawnie użyć Body() dla każdego pola
    - D) W POST wszystkie parametry są domyślnie z body

14. **[średnie] [API]** Gdy endpoint GET zwracający pojedynczy zasób po id nie znajdzie go (np. indeks poza zakresem), dobrą praktyką jest:
    - A) Zwrócić 200 OK z pustą treścią
    - B) Zwrócić 500 Internal Server Error
    - C) Zwrócić odpowiedź HTML z informacją o błędzie
    - D) Zwrócić odpowiedź z kodem 404 (np. PlainTextResponse lub JSON) i czytelnym komunikatem

15. **[trudne] [API]** Do czego w aplikacji webowej służy hashowanie haseł (np. bcrypt) przed zapisem do bazy?
    - A) Do szyfrowania sesji użytkownika
    - B) Do generowania tokenów JWT
    - C) Do jednokierunkowego przekształcenia hasła – zapisujemy tylko hash; przy logowaniu weryfikujemy hasło względem hasha (np. passlib/bcrypt)
    - D) Do podpisywania cookie

16. **[łatwe] [FastAPI]** Do czego służy parametr tags w APIRouter(prefix='/user', tags=['user'])?
    - A) Zmienia ścieżki URL (np. /user/get)
    - B) Grupuje endpointy w dokumentacji (np. Swagger UI); nie wpływa na URL ani zachowanie
    - C) Ustawia domyślny response_model dla endpointów
    - D) Włącza autoryzację dla endpointów

17. **[trudne] [Pydantic]** Schemat Pydantic ma pole zagnieżdżone (np. user: User), a obiekt ORM ma relację relationship do powiązanego obiektu. Jak możliwa jest serializacja do JSON z zagnieżdżonym obiektem?
    - A) Dzięki orm_mode (from_attributes) i relacji w ORM Pydantic/FastAPI odczytują zagnieżdżony obiekt i serializują go według zagnieżdżonego schematu
    - B) Router musi ręcznie dołączyć powiązany obiekt i wstawić go do słownika
    - C) Pole zagnieżdżone musi być opcjonalne i domyślnie None
    - D) Trzeba osobno wywołać zapytanie o powiązany obiekt i wstawić wynik do odpowiedzi

18. **[średnie] [SQLAlchemy]** Do czego służy back_populates w relationship SQLAlchemy?
    - A) Tworzy dodatkową kolumnę w bazie
    - B) Łączy dwie strony relacji (np. user.items i article.user), dzięki czemu z obu obiektów można przejść do powiązanych
    - C) Wymusza kaskadowe usuwanie (np. usunięcie usera usuwa artykuły)
    - D) Włącza lazy loading (domyślnie i tak jest włączony w wielu przypadkach)

19. **[trudne] [FastAPI]** Funkcja zarejestrowana jako exception_handler dla własnego wyjątku w FastAPI (app.exception_handler(MyException)) musi zwrócić:
    - A) Tylko numer statusu (int)
    - B) Tylko body (str)
    - C) Obiekt Response (np. JSONResponse(status_code=..., content={...})); może to być dowolny kod i treść, spójna dla wszystkich rzuceń tego wyjątku
    - D) Instancję HTTPException

20. **[trudne] [SQLAlchemy]** Dlaczego przy aktualizacji rekordu w SQLAlchemy ORM zmiany wprowadza się przez przypisanie atrybutów (obiekt.pole = wartość), a nie przez metodę update()?
   - A) update() jest usunięta w SQLAlchemy 2
   - B) update() wymaga słownika ze wszystkimi kolumnami
   - C) W ORM używa się wyłącznie raw SQL do aktualizacji
   - D) Obiekty mapowane (modele ORM) nie mają metody update(); zmiany robi się przez przypisanie atrybutów i db.commit(), a ORM śledzi zmiany i generuje UPDATE
