# Quiz FastAPI - 05.02.2026

## Pytanie 1
Co oznacza `prefix` w `APIRouter(prefix='/blog')`?
- A) Maksymalną długość URL
- B) Wspólny fragment URL dla wszystkich endpointów w routerze
- C) Minimalną długość URL
- D) Opcjonalny parametr bez znaczenia

## Pytanie 2
Jak FastAPI rozpoznaje, czy parametr funkcji to Path, Query czy Body?
- A) Na podstawie kolejności parametrów
- B) Na podstawie kontekstu (obecność w URL, typ BaseModel, etc.)
- C) Na podstawie nazwy parametru
- D) Zawsze jest to Query

## Pytanie 3
Co zwróci `db.query(DbUser).filter(DbUser.id == id).first()`, jeśli użytkownik o podanym ID nie istnieje?
- A) Pustą listę `[]`
- B) `None`
- C) Błąd `AttributeError`
- D) Pusty string `""`

## Pytanie 4
Dlaczego używamy osobnych schematów `UserBase` i `UserDisplay`?
- A) Aby zwiększyć wydajność
- B) Aby nie zwracać hasła w odpowiedzi API (bezpieczeństwo)
- C) Aby zmniejszyć rozmiar odpowiedzi
- D) To nie jest konieczne

## Pytanie 5
Co robi `orm_mode = True` w schemacie Pydantic?
- A) Włącza tryb ORM w bazie danych
- B) Pozwala na konwersję z obiektów SQLAlchemy do Pydantic
- C) Wyłącza walidację
- D) Zmienia typ danych

## Pytanie 6
Jakie metody HTTP obsługuje FastAPI dla operacji CRUD?
- A) GET (read), POST (create), PUT (update), DELETE (delete)
- B) GET (read), POST (create/update), DELETE (delete)
- C) Tylko POST dla wszystkich operacji
- D) GET, POST, PUT, PATCH, DELETE

## Pytanie 7
Co się stanie, jeśli spróbujemy usunąć użytkownika, który nie istnieje, bez sprawdzenia `if not db_user`?
- A) Zwróci `None`
- B) Zwróci pusty słownik `{}`
- C) Wystąpi błąd `UnmappedInstanceError`
- D) Nic się nie stanie

## Pytanie 8
Jak aktualizujemy dane w SQLAlchemy?
- A) Używamy metody `update()` na obiekcie
- B) Bezpośrednio przypisujemy wartości do atrybutów obiektu
- C) Używamy metody `set()`
- D) Nie można aktualizować danych

## Pytanie 9
Co robi `Hash.bcrypt(password)`?
- A) Deszyfruje hasło
- B) Hashuje hasło używając algorytmu bcrypt
- C) Porównuje hasła
- D) Zwraca hasło w plaintext

## Pytanie 10
Co oznacza `response_model=UserDisplay` w dekoratorze endpointu?
- A) Format odpowiedzi będzie zgodny ze schematem `UserDisplay`
- B) Wymusza użycie metody POST
- C) Waliduje tylko request
- D) Nie ma znaczenia

## Pytanie 11
Jak obsługujemy błąd, gdy użytkownik nie istnieje w endpointzie?
- A) Zwracamy `None`
- B) Używamy `raise HTTPException(status_code=404, detail='User not found')`
- C) Zwracamy pusty słownik
- D) Nie obsługujemy tego przypadku

## Pytanie 12
Co robi `db.refresh(db_user)`?
- A) Odświeża obiekt z bazy danych (pobiera najnowsze dane)
- B) Usuwa obiekt z bazy
- C) Tworzy nowy obiekt
- D) Zapisuje zmiany do bazy

## Pytanie 13
Jaka jest różnica między `db.add()` a `db.commit()`?
- A) `add()` zapisuje do bazy, `commit()` tylko dodaje do sesji
- B) `add()` dodaje do sesji, `commit()` zapisuje do bazy
- C) Oba robią to samo
- D) `add()` usuwa, `commit()` dodaje

## Pytanie 14
Co to jest Dependency Injection w kontekście FastAPI?
- A) Mechanizm wstrzykiwania zależności (np. sesji bazy danych) jako parametrów
- B) Sposób na importowanie modułów
- C) Metoda szyfrowania danych
- D) Typ walidacji

## Pytanie 15
Jakie są korzyści architektury warstwowej użytej w projekcie?
- A) Tylko separacja odpowiedzialności
- B) Separacja odpowiedzialności, łatwość testowania, łatwość utrzymania, bezpieczeństwo
- C) Tylko wydajność
- D) Brak korzyści

## Pytanie 16
Co zwraca `db.query(DbUser).all()`?
- A) Pierwszy obiekt z bazy
- B) Listę wszystkich obiektów `DbUser`
- C) Słownik z danymi
- D) `None`

## Pytanie 17
Dlaczego używamy `connect_args={"check_same_thread": False}` w konfiguracji SQLite?
- A) Aby zwiększyć bezpieczeństwo
- B) Aby umożliwić użycie SQLite w wielu wątkach (wymagane dla FastAPI)
- C) Aby zmniejszyć wydajność
- D) To nie jest konieczne

## Pytanie 18
Co oznacza `...` w `Body(..., min_length=10)`?
- A) Pole opcjonalne
- B) Pole wymagane
- C) Pole z wartością domyślną
- D) Pole z wieloma wartościami

## Pytanie 19
Jakie są typy parametrów w FastAPI?
- A) Tylko Path i Query
- B) Path, Query, Body, Depends
- C) Tylko Body
- D) Path, Query, Body, Header, Cookie

## Pytanie 20
Co robi `List[UserDisplay]` jako `response_model`?
- A) Wskazuje, że odpowiedź to lista obiektów `UserDisplay`
- B) Ogranicza liczbę elementów w liście
- C) Sortuje listę
- D) Filtruje listę
